namespace widgetassertions ;

private import Testing ;

class ListViews_ViewTest {
  
  private sut : ListViews_ViewModel  ;
  
  private testEnvironment : ListViews_ViewModelTestEnvironment  ;
  
  
  protected BeforeTest (  ) {
    this.testEnvironment = new ListViews_ViewModelTestEnvironmentImpl (  );

    this.testEnvironment.Init (  );

  }
  
  
  public My_Scenario_given_empty_context_when_then_MyListOfLabel_has_4_rows_and_selected_row_index_3_and_MyListOfImages_has_2_rows_and_MyCheckboxes_has_3_rows_and_MyStringRowHandles_has_2_rows_and_selected_row_handle_ROW1_and_MyListWithMultiRowSelection_has_3_rows_and_selected_row_indices_0_2_ (  ) {
    this.given_empty_context (  );

    this.BuildSut (  );

    this.then_MyListOfLabel_has_4_rows_and_selected_row_index_3 (  );

    this.then_MyListOfImages_has_2_rows (  );

    this.then_MyCheckboxes_has_3_rows (  );

    this.then_MyStringRowHandles_has_2_rows_and_selected_row_handle_ROW1 (  );

    this.then_MyListWithMultiRowSelection_has_3_rows_and_selected_row_indices_0_2_ (  );

  }
  
  public List_Ignore_Column_given_empty_context_when_then_MyListOfLabel_has_4_rows (  ) {
    this.given_empty_context (  );

    this.BuildSut (  );

    this.then_MyListOfLabel_has_4_rows (  );

  }
  
  public List_Ignore_Cell_given_empty_context_when_then_MyListOfLabel_has_4_rows (  ) {
    this.given_empty_context (  );

    this.BuildSut (  );

    this.then_MyListOfLabel_has_4_rows_1 (  );

  }
  
  public List_of_Buttons_given_when_then_MyButtons_has_0_rows (  ) {
    this.BuildSut (  );

    this.then_MyButtons_has_0_rows (  );

  }
  
  
  protected BuildSut (  ) {
    this.sut = this.testEnvironment.BuildSut (  );

  }
  
  
  // Given Helper Definitions

  
  public given_empty_context (  ) {    
  }
  
  // When Helper Definitions

  
  // Then Helper Definitions

  
  public then_MyListOfLabel_has_4_rows_and_selected_row_index_3 (  ) {
    let actualRows: ListViews_ViewModelMyListOfLabelRow [] = this.sut.getMyListOfLabelListRows (  );

    AssertEqualWithMessage ( 4, actualRows->size (  ), "Expected that list view MyListOfLabel has 4 rows, but has " + actualRows->size (  ) );

    {
      let row0: ListViews_ViewModelMyListOfLabelRow = actualRows[ 1 ];

      AssertEqualWithMessage ( 0, row0.getRowIndex (  ), "Expected that list view MyListOfLabel row at index 0 has rowhandle <0>, but was <" + row0.getRowIndex (  ) + ">" );

      AssertEqualWithMessage ( "A", row0.getMyValueLabelText (  ), "Expected that label My Value has text <" + "A" + ">, but was <" + row0.getMyValueLabelText (  ) + ">" );

      AssertFalseWithMessage ( row0.getIsMyValueLabelVisible (  ), "Expected label My Value is hidden, but it was visible" );

      AssertTrueWithMessage ( row0.getIsMyValueLabelEnabled (  ), "Expected label My Value is enabled, but it was disabled" );

    }

    {
      let row1: ListViews_ViewModelMyListOfLabelRow = actualRows[ 2 ];

      AssertEqualWithMessage ( 1, row1.getRowIndex (  ), "Expected that list view MyListOfLabel row at index 1 has rowhandle <1>, but was <" + row1.getRowIndex (  ) + ">" );

      AssertEqualWithMessage ( "B", row1.getMyValueLabelText (  ), "Expected that label My Value has text <" + "B" + ">, but was <" + row1.getMyValueLabelText (  ) + ">" );

      AssertFalseWithMessage ( row1.getIsMyValueLabelEnabled (  ), "Expected label My Value is disabled, but it was enabled" );

    }

    {
      let row2: ListViews_ViewModelMyListOfLabelRow = actualRows[ 3 ];

      AssertEqualWithMessage ( 2, row2.getRowIndex (  ), "Expected that list view MyListOfLabel row at index 2 has rowhandle <2>, but was <" + row2.getRowIndex (  ) + ">" );

      AssertEqualWithMessage ( "C", row2.getMyValueLabelText (  ), "Expected that label My Value has text <" + "C" + ">, but was <" + row2.getMyValueLabelText (  ) + ">" );

      AssertTrueWithMessage ( row2.getIsMyValueLabelEnabled (  ), "Expected label My Value is enabled, but it was disabled" );

    }

    {
      let row3: ListViews_ViewModelMyListOfLabelRow = actualRows[ 4 ];

      AssertEqualWithMessage ( 3, row3.getRowIndex (  ), "Expected that list view MyListOfLabel row at index 3 has rowhandle <3>, but was <" + row3.getRowIndex (  ) + ">" );

      AssertEqualWithMessage ( "D", row3.getMyValueLabelText (  ), "Expected that label My Value has text <" + "D" + ">, but was <" + row3.getMyValueLabelText (  ) + ">" );

      AssertTrueWithMessage ( row3.getIsMyValueLabelVisible (  ), "Expected label My Value is visible, but it was hidden" );

    }

    AssertEqualWithMessage ( 3, this.sut.getMyListOfLabelListSelectedRow (  ), "Expected that list view MyListOfLabel has selected row with row handle <" + 3 + ">, but was <" + this.sut.getMyListOfLabelListSelectedRow (  ) + ">" );

  }
  
  public then_MyListOfImages_has_2_rows (  ) {
    let actualRows: ListViews_ViewModelMyListOfImagesRow [] = this.sut.getMyListOfImagesListRows (  );

    AssertEqualWithMessage ( 2, actualRows->size (  ), "Expected that list view MyListOfImages has 2 rows, but has " + actualRows->size (  ) );

    {
      let row0: ListViews_ViewModelMyListOfImagesRow = actualRows[ 1 ];

      AssertEqualWithMessage ( 0, row0.getRowIndex (  ), "Expected that list view MyListOfImages row at index 0 has rowhandle <0>, but was <" + row0.getRowIndex (  ) + ">" );

      AssertEqualWithMessage ( "image_star", row0.getMyInfoImageName (  ), "Expected that image My Info has " + "image <image_star>" + ", but was <" + row0.getMyInfoImageName (  ) + ">" );

    }

    {
      let row1: ListViews_ViewModelMyListOfImagesRow = actualRows[ 2 ];

      AssertEqualWithMessage ( 1, row1.getRowIndex (  ), "Expected that list view MyListOfImages row at index 1 has rowhandle <1>, but was <" + row1.getRowIndex (  ) + ">" );

      AssertEqualWithMessage ( "image_circle", row1.getMyInfoImageName (  ), "Expected that image My Info has " + "image <image_circle>" + ", but was <" + row1.getMyInfoImageName (  ) + ">" );

    }

  }
  
  public then_MyCheckboxes_has_3_rows (  ) {
    let actualRows: ListViews_ViewModelMyCheckboxesRow [] = this.sut.getMyCheckboxesListRows (  );

    AssertEqualWithMessage ( 3, actualRows->size (  ), "Expected that list view MyCheckboxes has 3 rows, but has " + actualRows->size (  ) );

    {
      let row0: ListViews_ViewModelMyCheckboxesRow = actualRows[ 1 ];

      AssertEqualWithMessage ( 0, row0.getRowIndex (  ), "Expected that list view MyCheckboxes row at index 0 has rowhandle <0>, but was <" + row0.getRowIndex (  ) + ">" );

      ;

    }

    {
      let row1: ListViews_ViewModelMyCheckboxesRow = actualRows[ 2 ];

      AssertEqualWithMessage ( 1, row1.getRowIndex (  ), "Expected that list view MyCheckboxes row at index 1 has rowhandle <1>, but was <" + row1.getRowIndex (  ) + ">" );

      ;

    }

    {
      let row2: ListViews_ViewModelMyCheckboxesRow = actualRows[ 3 ];

      AssertEqualWithMessage ( 2, row2.getRowIndex (  ), "Expected that list view MyCheckboxes row at index 2 has rowhandle <2>, but was <" + row2.getRowIndex (  ) + ">" );

      ;

      AssertEqualWithMessage ( "My Label", row2.getHeaderCheckBoxText (  ), "Expected that checkbox Header has text <" + "My Label" + ">, but was <" + row2.getHeaderCheckBoxText (  ) + ">" );

    }

  }
  
  public then_MyStringRowHandles_has_2_rows_and_selected_row_handle_ROW1 (  ) {
    let actualRows: ListViews_ViewModelMyStringRowHandlesRow [] = this.sut.getMyStringRowHandlesListRows (  );

    AssertEqualWithMessage ( 2, actualRows->size (  ), "Expected that list view MyStringRowHandles has 2 rows, but has " + actualRows->size (  ) );

    {
      let row0: ListViews_ViewModelMyStringRowHandlesRow = actualRows[ 1 ];

      AssertEqualWithMessage ( "ROW0", row0.getRowHandle (  ), "Expected that list view MyStringRowHandles row at index 0 has rowhandle <ROW0>, but was <" + row0.getRowHandle (  ) + ">" );

      AssertEqualWithMessage ( "A", row0.getValuesLabelText (  ), "Expected that label Values has text <" + "A" + ">, but was <" + row0.getValuesLabelText (  ) + ">" );

    }

    {
      let row1: ListViews_ViewModelMyStringRowHandlesRow = actualRows[ 2 ];

      AssertEqualWithMessage ( "ROW1", row1.getRowHandle (  ), "Expected that list view MyStringRowHandles row at index 1 has rowhandle <ROW1>, but was <" + row1.getRowHandle (  ) + ">" );

      AssertEqualWithMessage ( "B", row1.getValuesLabelText (  ), "Expected that label Values has text <" + "B" + ">, but was <" + row1.getValuesLabelText (  ) + ">" );

    }

    AssertEqualWithMessage ( "ROW1", this.sut.getMyStringRowHandlesListSelectedRow (  ), "Expected that list view MyStringRowHandles has selected row with row handle <" + "ROW1" + ">, but was <" + this.sut.getMyStringRowHandlesListSelectedRow (  ) + ">" );

  }
  
  public then_MyListWithMultiRowSelection_has_3_rows_and_selected_row_indices_0_2_ (  ) {
    let actualRows: ListViews_ViewModelMyListWithMultiRowSelectionRow [] = this.sut.getMyListWithMultiRowSelectionListRows (  );

    AssertEqualWithMessage ( 3, actualRows->size (  ), "Expected that list view MyListWithMultiRowSelection has 3 rows, but has " + actualRows->size (  ) );

    {
      let row0: ListViews_ViewModelMyListWithMultiRowSelectionRow = actualRows[ 1 ];

      AssertEqualWithMessage ( 0, row0.getRowIndex (  ), "Expected that list view MyListWithMultiRowSelection row at index 0 has rowhandle <0>, but was <" + row0.getRowIndex (  ) + ">" );

      AssertEqualWithMessage ( "A", row0.getHeaderLabelText (  ), "Expected that label Header has text <" + "A" + ">, but was <" + row0.getHeaderLabelText (  ) + ">" );

    }

    {
      let row1: ListViews_ViewModelMyListWithMultiRowSelectionRow = actualRows[ 2 ];

      AssertEqualWithMessage ( 1, row1.getRowIndex (  ), "Expected that list view MyListWithMultiRowSelection row at index 1 has rowhandle <1>, but was <" + row1.getRowIndex (  ) + ">" );

      AssertEqualWithMessage ( "B", row1.getHeaderLabelText (  ), "Expected that label Header has text <" + "B" + ">, but was <" + row1.getHeaderLabelText (  ) + ">" );

    }

    {
      let row2: ListViews_ViewModelMyListWithMultiRowSelectionRow = actualRows[ 3 ];

      AssertEqualWithMessage ( 2, row2.getRowIndex (  ), "Expected that list view MyListWithMultiRowSelection row at index 2 has rowhandle <2>, but was <" + row2.getRowIndex (  ) + ">" );

      AssertEqualWithMessage ( "C", row2.getHeaderLabelText (  ), "Expected that label Header has text <" + "C" + ">, but was <" + row2.getHeaderLabelText (  ) + ">" );

    }

    let actualMyListWithMultiRowSelectionSelectedRowHandles: Integer [] = this.sut.getMyListWithMultiRowSelectionListSelectedRows (  );

    AssertEqualWithMessage ( 2, actualMyListWithMultiRowSelectionSelectedRowHandles->size (  ), "Expected that list view MyListWithMultiRowSelection has selected 2 rows, but has " + actualMyListWithMultiRowSelectionSelectedRowHandles->size (  ) );

    AssertEqualWithMessage ( 0, actualMyListWithMultiRowSelectionSelectedRowHandles[ 1 ] ?? 0, "Expected that list view MyListWithMultiRowSelection has selected row at index 0 with row handle <" + 0 + ">, but was <" + actualMyListWithMultiRowSelectionSelectedRowHandles[ 1 ] ?? 0 + ">" );

    AssertEqualWithMessage ( 2, actualMyListWithMultiRowSelectionSelectedRowHandles[ 2 ] ?? 0, "Expected that list view MyListWithMultiRowSelection has selected row at index 1 with row handle <" + 2 + ">, but was <" + actualMyListWithMultiRowSelectionSelectedRowHandles[ 2 ] ?? 0 + ">" );

  }
  
  public then_MyListOfLabel_has_4_rows (  ) {
    let actualRows: ListViews_ViewModelMyListOfLabelRow [] = this.sut.getMyListOfLabelListRows (  );

    AssertEqualWithMessage ( 4, actualRows->size (  ), "Expected that list view MyListOfLabel has 4 rows, but has " + actualRows->size (  ) );

    {
      let row0: ListViews_ViewModelMyListOfLabelRow = actualRows[ 1 ];

      AssertEqualWithMessage ( 0, row0.getRowIndex (  ), "Expected that list view MyListOfLabel row at index 0 has rowhandle <0>, but was <" + row0.getRowIndex (  ) + ">" );

    }

    {
      let row1: ListViews_ViewModelMyListOfLabelRow = actualRows[ 2 ];

      AssertEqualWithMessage ( 1, row1.getRowIndex (  ), "Expected that list view MyListOfLabel row at index 1 has rowhandle <1>, but was <" + row1.getRowIndex (  ) + ">" );

    }

    {
      let row2: ListViews_ViewModelMyListOfLabelRow = actualRows[ 3 ];

      AssertEqualWithMessage ( 2, row2.getRowIndex (  ), "Expected that list view MyListOfLabel row at index 2 has rowhandle <2>, but was <" + row2.getRowIndex (  ) + ">" );

    }

    {
      let row3: ListViews_ViewModelMyListOfLabelRow = actualRows[ 4 ];

      AssertEqualWithMessage ( 3, row3.getRowIndex (  ), "Expected that list view MyListOfLabel row at index 3 has rowhandle <3>, but was <" + row3.getRowIndex (  ) + ">" );

    }

  }
  
  public then_MyListOfLabel_has_4_rows_1 (  ) {
    let actualRows: ListViews_ViewModelMyListOfLabelRow [] = this.sut.getMyListOfLabelListRows (  );

    AssertEqualWithMessage ( 4, actualRows->size (  ), "Expected that list view MyListOfLabel has 4 rows, but has " + actualRows->size (  ) );

    {
      let row0: ListViews_ViewModelMyListOfLabelRow = actualRows[ 1 ];

      AssertEqualWithMessage ( 0, row0.getRowIndex (  ), "Expected that list view MyListOfLabel row at index 0 has rowhandle <0>, but was <" + row0.getRowIndex (  ) + ">" );

    }

    {
      let row1: ListViews_ViewModelMyListOfLabelRow = actualRows[ 2 ];

      AssertEqualWithMessage ( 1, row1.getRowIndex (  ), "Expected that list view MyListOfLabel row at index 1 has rowhandle <1>, but was <" + row1.getRowIndex (  ) + ">" );

      AssertEqualWithMessage ( "B", row1.getMyValueLabelText (  ), "Expected that label My Value has text <" + "B" + ">, but was <" + row1.getMyValueLabelText (  ) + ">" );

      AssertFalseWithMessage ( row1.getIsMyValueLabelEnabled (  ), "Expected label My Value is disabled, but it was enabled" );

    }

    {
      let row2: ListViews_ViewModelMyListOfLabelRow = actualRows[ 3 ];

      AssertEqualWithMessage ( 2, row2.getRowIndex (  ), "Expected that list view MyListOfLabel row at index 2 has rowhandle <2>, but was <" + row2.getRowIndex (  ) + ">" );

    }

    {
      let row3: ListViews_ViewModelMyListOfLabelRow = actualRows[ 4 ];

      AssertEqualWithMessage ( 3, row3.getRowIndex (  ), "Expected that list view MyListOfLabel row at index 3 has rowhandle <3>, but was <" + row3.getRowIndex (  ) + ">" );

      AssertEqualWithMessage ( "D", row3.getMyValueLabelText (  ), "Expected that label My Value has text <" + "D" + ">, but was <" + row3.getMyValueLabelText (  ) + ">" );

      AssertTrueWithMessage ( row3.getIsMyValueLabelVisible (  ), "Expected label My Value is visible, but it was hidden" );

    }

  }
  
  public then_MyButtons_has_0_rows (  ) {
    let actualRows: ListViews_ViewModelMyButtonsRow [] = this.sut.getMyButtonsListRows (  );

    AssertEqualWithMessage ( 0, actualRows->size (  ), "Expected that list view MyButtons has 0 rows, but has " + actualRows->size (  ) );

  }
}